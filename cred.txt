
1. Neo4j
NEO4J_URI = "neo4j+s://d333bb5f.databases.neo4j.io"  
NEO4J_USER = "neo4j"                                  
NEO4J_PASSWORD = "ULBfmPb5dsCbkEjzQpdfvGroJvnW28MFDXYdGC6p9m4"


2. LLM provider credentials
OPENAI_API_KEY=<YOUR_KEY>
LLM_MODEL=gpt-3.5-turbo
EMBEDDING_MODEL=text-embedding-3-small


3. Embedding model choice
 openAI text-embedding-3-small (1536 dimensions)



4. added pdf in the workspace


5. Expected UX Behavior for the Chatbot

No citations

Smooth, natural, mentor-style responses

Must use ONLY course chunk context (no hallucination)

Maximum answer length: 3–6 sentences

No streaming required (optional for later)

Should display loading state in the frontend until answer returns



6. Infrastructure Constraints

The RAG system must run inside the existing Express + TypeScript backend

Backend stack:

Node 20

Express

TypeScript

Prisma

PostgreSQL

JWT auth

Google OAuth




7. Security & Policy Requirements

Do not store user prompts or LLM outputs in the database

Log only minimal usage metadata: timestamp + user_id + status (success/fail)

Add basic rate limiting (per user)

Remove any PII before sending to OpenAI

Hide all secrets inside .env, never in code

NEVER commit keys in Git